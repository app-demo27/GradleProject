node {
   stage('Build') {
      // Run the maven build
           bat 'gradle clean build'
          
   }
   stage('Results') {
      junit '**/target/surefire-reports/TEST-*.xml'
      archiveArtifacts 'build/lib/*.jar'
   }
}
